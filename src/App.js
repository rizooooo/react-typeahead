import React from 'react';
import { LandingModule } from './modules';

// Insert global providers in here
// Render switch on react-router-dom if have multiple routes, but for now just render a single router module
// <Switch>
//     <Route />
// </Switch>

const App = () => <LandingModule />

export default App;